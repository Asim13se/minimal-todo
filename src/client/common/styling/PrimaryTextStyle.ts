import {TextStyle} from 'react-native';
import PrimaryFontFamily from './PrimaryFontFamily';
import Colors from './Colors';

const PrimaryTextStyle: TextStyle = {
  fontFamily: PrimaryFontFamily,
  fontSize: 15,
  color: Colors.text1,
};

export default PrimaryTextStyle;
